[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "npm install --prefix server",
]

[phases.build]
cmds = [
  "VITE_API_URL=/api npm run build:prod",
]

[start]
cmd = "npm run start:prod"
