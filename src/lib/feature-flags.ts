/** Compra de créditos e assinatura de planos no frontend. */
export const CREDITS_AND_PLANS_ENABLED = true

/** Radar de captação no frontend. Código permanece; `false` deixa invisível no menu e nas entradas. */
export const CAPTURE_RADAR_ENABLED = false

/** Assistente FoxAi no frontend. Código permanece; `false` deixa invisível no menu, dashboard e widget. */
export const FOX_AI_ENABLED = false
